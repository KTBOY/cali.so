- 所有环境变量通过根 `env.mjs` 用 Zod 分 server/client 两版 schema 校验，并以 Proxy 阻止客户端访问服务端变量。
- 跨模块引用统一使用 `~/` 路径别名，避免相对路径穿越。
- 对外暴露的配置/单例（KV Key、Redis、Email）放在 `~/config` 与 `~/lib`，由调用方直接 import 而非通过参数注入。
- 数据库 schema 与 Drizzle migrations 分离于 `db/schema.ts` 与 `db/migrations`，通过 `drizzle-kit` 驱动生成。