- 外部服务（Redis、Resend）以模块级单例形式在各自文件中 `new` 一次后直接 `export const`，调用方按需 import 使用而非传入实例。
- 环境相关配置统一通过 `import { env } from '~/env.mjs'` 读取，并按 `VERCEL_ENV` / `NODE_ENV` 条件分支选择生产或本地回退值。
- 可复用的键名/路由等常量集中在 `config/` 下以 `as const` 对象或数组导出，避免在业务代码中散落硬编码字符串。