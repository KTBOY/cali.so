- 每个实体在 `schema.ts` 中以 `pgTable` 定义表结构，并在需要时通过 `(table) => ({ ... })` 形式追加索引。
- 对外暴露的 DTO 均配套一个 Zod schema（`XxxDtoSchema`）与 `z.infer` 派生的 TypeScript 类型，并以同名 `Hashids` 实例负责 id 编解码。
- 查询函数位于 `queries/<entity>.ts` 中，统一从 `~/db` 导入 `db` 实例，返回结果前对 id 调用对应实体的 `Hashids.encode` 进行混淆。